# Local env / secrets
.env
.env.* 
# Node / PHP deps
node_modules/
vendor/
# IDE / OS
.DS_Store
.idea/
.vscode/
# Keys / certs
*.pem
*.key
*.crt
# uploads and build artifacts
uploads/
wp-content/uploads/
*.log
# Composer / npm lock if you want to ignore (optional)
# composer.lock
